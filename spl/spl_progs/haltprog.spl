breakpoint;
[PROCESS_TABLE + [SYSTEM_STATUS_TABLE + 1] * 16 + 4] = TERMINATED;
alias iterator R1;
iterator = 1;
alias check R3;
check = 0;
while (iterator < 16) do 
    if ([PROCESS_TABLE + iterator * 16 + 4] != TERMINATED) then
        check = check + 1; 
    endif; 
    iterator = iterator + 1;
endwhile;
breakpoint; 
if (check >= 1) then 
    call MOD_5;  
endif; 
halt;